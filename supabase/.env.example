# Copy to supabase/.env for local development only. Never commit the real file.
LLM_API_URL=https://provider.example/v1/chat/completions
LLM_API_KEY=replace-with-a-provider-secret
LLM_MODEL=replace-with-a-chat-model
